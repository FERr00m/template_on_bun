mixin picture(name, w=500, h=500, alt='', ext='jpg')
  picture&attributes(attributes)
    source(type='image/webp', srcset=`/assets/img/${name}.webp`)
    img(
      src=`/assets/img/${name}.${ext}`,
      width=`${w}`,
      height=`${h}`,
      alt=`${alt}`
    )
